# 업무 관리 지침

이 Workspace에서 Claude Code는 아래 규칙을 따른다.

## 작업 시작 전 확인

- 사용자의 발언을 결정으로 단정하지 않는다. 질문, 아이디어 제안, 의견 확인, 생각 정리는 결정이 아니다.
- 결정으로 판단되는 내용이라도 문서를 수정하기 전에 무엇을 어느 파일에 어떻게 반영할지 먼저 알리고 진행 여부를 확인한다.
- 사용자가 진행을 승인한 뒤에 문서를 수정한다.
- 다음 작업은 사용자 확인 없이 수행하지 않는다.
  - `02_결정사항.md`에 결정 기록
  - 미결 사항의 상태 변경
  - `00_기준문서` 수정
  - 정책서, PRD, 기능설계서, 기술설계서, 화면기획 등 산출물 수정
  - 업무 폴더 생성, 칸반 카드 등록·이동
- 조회, 분석, 비교, 설명, 방안 제시는 확인 없이 수행한다. 수행기록은 승인받은 작업을 완료한 뒤에 남긴다.
- 한 번의 승인은 그 작업에만 적용한다. 이어지는 다른 작업은 다시 확인한다.

## 기준문서

- `01_Arion/00_기준문서`는 프로젝트 전체의 개요, 컨셉, 아키텍처 등 모든 업무가 공통으로 따르는 기준 자료를 관리한다.
- 업무를 수행할 때는 관련 기준문서를 먼저 확인하고, 업무 산출물이 기준문서와 어긋나지 않도록 한다.
- 업무 중 기준문서와 충돌하거나 기준문서를 바꿔야 하는 판단이 나오면 임의로 수정하지 않고 사용자에게 알린다.
- 기준문서 변경이 승인되면 기준문서를 수정하고, 변경 사유를 해당 업무의 `02_결정사항.md`에 기록한다.
- 기준문서에는 업무별 상세 내용을 옮겨 적지 않고, 프로젝트 전체에 적용되는 내용만 남긴다.

## 새 업무 생성

1. `01_Arion/02_템플릿/업무폴더_복사용`을 복사하여 `01_Arion/01_업무/NN_업무명` 폴더를 만든다.
2. 루트 `00_전체업무.kanban`에 `업무명` 형식으로 카드를 등록한다.
3. 사용자가 상태를 별도로 지정하지 않으면 새 카드는 반드시 `백로그`에 등록한다.
4. 사용자가 진행 중, 완료, 보류를 명시한 경우에만 해당 컬럼에 등록한다.
5. 칸반 컬럼은 `백로그`, `진행 중`, `완료`, `보류`만 사용하며 임의로 추가하거나 이름을 변경하지 않는다.
6. 프로젝트 하위에 별도의 칸반 파일을 만들지 않는다.

## 날짜별 누적 기록

- 날짜 기준은 한국시간(Asia/Seoul)의 `YYYY-MM-DD`를 사용한다.
- 결정사항, 회의메모, 업무수행기록은 각각 기존 파일 하나에 계속 누적한다.
- 날짜별 새 파일을 만들거나 기존 파일을 날짜별 파일로 분리하지 않는다.
- 최신 날짜를 파일 상단에 추가한다.
- 결정사항과 회의메모는 같은 날짜에 서로 다른 항목이 있으면 해당 날짜 아래에 `### 제목`을 추가한다.
- 세부 `04_업무수행기록.md`는 같은 날짜에 작업할 때마다 `### HH:mm 작업명`을 추가하여 수행 과정과 판단 변화를 보존한다.
- 루트 `01_전체업무수행기록.md`는 같은 날짜의 동일 업무 영역을 누적 결과로 갱신하는 일일 요약으로 관리한다.
- 이전 날짜의 기록을 삭제하거나 덮어쓰지 않는다.

## 문서별 기록 기준

- `02_결정사항.md`: 사용자가 확정하거나 승인한 의사결정만 기록한다. 결정 내용, 배경·근거, 영향 범위, 후속 조치를 포함한다.
- `03_회의메모.md`: 사용자가 제공한 회의 내용이나 녹취를 기준으로 기록한다. 참석자, 논의 내용, 결정사항, 후속 조치를 포함한다.
- `04_업무수행기록.md`: Claude Code가 의미 있는 작업 단위를 완료한 후 별도 요청이 없어도 기록한다. 오늘 날짜 영역이 없으면 새로 만들고, 이미 있으면 해당 날짜 아래에 `### HH:mm 작업명`을 추가한다. 수행 내용, 결과·산출물, 확인된 이슈, 다음 할 일을 포함한다.
- `04_업무수행기록.md` 상단에는 `## 미결 사항` 섹션을 두고 해당 업무의 미결 항목을 한곳에서 관리한다. 번호, 제목, 상태, 확인 주체를 요약 표로 작성하고, 항목마다 `### N. 제목` 아래에 배경, 확인이 필요한 내용, 현재 가정, 영향, 근거 문서를 작성한다.
- 미결 사항 섹션은 날짜별 기록보다 위에 위치하며, 현재 상태를 반영하여 계속 갱신한다.
- 미결 항목이 확정되면 상태를 `확정`으로 바꾸고 `02_결정사항.md`에 결정 내용을 기록한다. 항목 자체는 삭제하지 않는다.
- 날짜별 수행기록 안에는 미결 목록을 반복해서 적지 않는다.
- 루트 `01_전체업무수행기록.md`: Claude Code가 업무를 수행한 후 해당 업무의 상세 수행기록과 함께 반드시 갱신한다. 업무명, 수행 요약, 핵심 결과, 다음 할 일과 상세 기록의 상대 경로만 작성한다.
- 전체 업무 수행 기록에는 상세 기록을 그대로 복사하지 않는다.
- 같은 날짜에 여러 업무를 수행하면 `## YYYY-MM-DD` 아래에 `### 업무명`을 각각 추가한다.
- 같은 날짜에 동일한 업무를 여러 번 수행하면 기존 `### 업무명` 영역을 그날의 누적 결과로 갱신한다. 새 항목이나 중복 제목을 추가하지 않는다.
- 기준문서를 수정한 경우 관련 업무의 수행기록에 수정한 기준문서와 변경 요지를 남긴다. 특정 업무와 무관한 기준문서 작업은 전체 수행기록에 `### 기준문서`로 기록한다.
- 알 수 없는 회의 내용이나 결정사항을 추정하여 작성하지 않는다.

## 수행기록 생성 기준

다음 중 하나가 발생하면 세부 수행기록을 추가하고 전체 수행기록을 갱신한다.

- 검토·분석 결과 결론이나 유의미한 판단이 나온 경우
- 문서, 화면기획, 설계, 코드, 분석자료 등 산출물을 생성하거나 의미 있게 수정한 경우
- 리뷰, 테스트 또는 검증 결과가 나온 경우
- 새로운 이슈, 제약사항 또는 차단 요인을 확인한 경우
- 업무를 전달하거나 하나의 작업 단위를 완료한 경우

단순 파일 열람, 변화가 없는 확인, 사소한 오탈자 수정, 일반적인 질의응답은 기록하지 않는다. 어느 업무에 해당하는지 불명확하면 기록 전에 사용자에게 확인한다.

## 업무 완료

- 업무가 완료되면 수행기록에 최종 결과와 산출물 위치를 남긴다.
- 전체 업무 칸반의 카드를 `완료`로 이동한다.
- 사용자가 보관을 요청하면 업무 폴더 전체를 `01_Arion/99_보관함`으로 이동한다.
- PM Toolkit 칸반은 컬럼의 카드 목록 접기를 지원하지 않으므로, 사용자가 완료 항목 정리를 요청하면 메인 칸반의 완료 카드를 루트 `02_완료업무보관.md`에 월별로 옮긴다.
- 완료 카드를 보관 파일로 옮겨도 메인 칸반의 `완료` 컬럼은 삭제하지 않는다.
